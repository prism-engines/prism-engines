# SINGLE SOURCE OF TRUTH FOR THRESHOLDS
exclusion_policy:
  mode: observe  # observe | warn | exclude
  failure_threshold: 0.50
  min_indicators_required: 5
  strictness_profile: balanced  # permissive | balanced | strict

# Audit settings
logging:
  log_exclusions: true
  log_level: INFO
