# ==============================================================================
# PRISM ENGINE - Complete Requirements (Unified 2025 Spec)
# Last revised: December 2025
# ==============================================================================

# Install:
#     pip install -r requirements.txt
#
# Fresh install:
#     python -m venv venv
#     source venv/bin/activate  (Mac/Linux)
#     venv\Scripts\activate     (Windows)
#     pip install --upgrade pip setuptools wheel
#     pip install -r requirements.txt
# ==============================================================================


# ------------------------------------------------------------------------------
# CORE DATA SCIENCE
# ------------------------------------------------------------------------------
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0


# ------------------------------------------------------------------------------
# MACHINE LEARNING & PATTERN DETECTION
# ------------------------------------------------------------------------------
scikit-learn>=1.3.0
hmmlearn>=0.3.0              # Regime Switching Lens
umap-learn>=0.5.0            # Dimensionality reduction (Consensus Lens)
tsfresh>=0.20.0              # Temporal feature extraction
numba>=0.57.0                # Computational acceleration
pykalman>=0.9.5              # Kalman filters (state smoothing)
ruptures>=1.1.7              # Changepoint detection (Seismometer)


# ------------------------------------------------------------------------------
# VISUALIZATION
# ------------------------------------------------------------------------------
matplotlib>=3.7.0
seaborn>=0.12.0


# ------------------------------------------------------------------------------
# DATA FETCHING & ASYNC PIPELINE
# ------------------------------------------------------------------------------
fredapi>=0.5.0
pandas-datareader>=0.10.0
yfinance>=0.2.28
requests>=2.28.0
httpx>=0.27.0                # Modern async HTTP client
aiohttp>=3.9.0               # Concurrent asynchronous fetching


# ------------------------------------------------------------------------------
# DATABASE & ORM
# ------------------------------------------------------------------------------
sqlalchemy>=2.0.0
pysqlite3-binary>=0.5.0      # More stable SQLite build for macOS/Linux
alembic>=1.13.1              # Schema migrations (registry evolution)


# ------------------------------------------------------------------------------
# CONFIGURATION, STRUCTURED DATA, UTILITIES
# ------------------------------------------------------------------------------
pyyaml>=6.0                  # Registry and YAML loaders
python-dotenv>=1.0.0         # Secrets management
pydantic>=2.5.3              # Typed config schemas


# ------------------------------------------------------------------------------
# STATISTICAL / ECONOMETRIC ANALYSIS
# ------------------------------------------------------------------------------
statsmodels>=0.14.0          # ADF, KPSS, PP tests, regression, etc.
arch>=6.0.0                  # GARCH & volatility modelling


# ------------------------------------------------------------------------------
# SIGNAL PROCESSING & GEOMETRIC TRANSFORMS
# ------------------------------------------------------------------------------
PyWavelets>=1.4.1            # Wavelet transforms (Wavelet Lens)
spectrum>=0.8.1              # Spectral density estimation
ripser>=0.6.4                # TDA Lens (topological signatures)
networkx>=3.1                # Influence & network geometry lenses


# ------------------------------------------------------------------------------
# PARALLEL COMPUTATION / HIGH PERFORMANCE
# ------------------------------------------------------------------------------
tqdm>=4.65.0                 # Progress bars
dask>=2024.1.0               # Parallel compute backend (future)
joblib>=1.3.0                # Parallelization utilities


# ------------------------------------------------------------------------------
# WEB / DASHBOARD (Phase 10+)
# ------------------------------------------------------------------------------
fastapi>=0.110.0
uvicorn>=0.29.0
jinja2>=3.1.0


# ------------------------------------------------------------------------------
# TESTING / DEVELOPMENT
# ------------------------------------------------------------------------------
pytest>=8.0.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0


# ==============================================================================
# NOTES
# ==============================================================================
# If dependency conflicts occur:
#     pip install --upgrade pip setuptools wheel
#     pip install -r requirements.txt --no-cache-dir
#
# For Apple Silicon (M1/M2/M3):
#     pip install numpy --no-binary :all:
#     or use conda to accelerate builds.
#
# TDA Lens:
#     ripser is lightweight; giotto-tda omitted for size but can be added.
# ==============================================================================

